# Copy to .env and fill in. The server also reads these straight from the environment.

# HTTP port
PORT=8080

# Path to the SQLite database file (created automatically)
DB_PATH=data/cv.db

# Directory for uploaded photo / resume (created automatically)
UPLOADS_DIR=data/uploads

# Admin panel password (CHANGE THIS — default is "admin" and triggers a warning)
ADMIN_PASSWORD=change-me-to-something-strong

# Secret used to sign admin session JWTs (base64, 32+ bytes). Generate with:
#   openssl rand -base64 32
# If unset, a random secret is generated on each boot (sessions won't survive a restart).
JWT_SECRET=

# AES-256 key used to encrypt sensitive DB fields at rest (Spotify tokens,
# profile email/phone/telegram). Must be base64, exactly 32 bytes decoded. Generate with:
#   openssl rand -base64 32
# Required — the server refuses to start without it (and prints a freshly
# generated one to help you set it up). KEEP IT SAFE AND BACKED UP: losing it
# makes encrypted data permanently unrecoverable.
ENCRYPTION_KEY=

# Telegram notifications for the contact form (optional).
# 1. Create a bot via @BotFather -> get the token.
# 2. Write to your bot once, then open
#    https://api.telegram.org/bot<token>/getUpdates to find your chat id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Spotify integration (optional): "now playing" + top tracks/artists.
# 1. Create an app at https://developer.spotify.com/dashboard.
# 2. Add SPOTIFY_REDIRECT_URI below to the app's Redirect URIs.
# 3. Connect your account once via the admin panel's Spotify tab.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=https://semenovm.ru/api/spotify/callback
